#include <bits/stdc++.h>
using namespace std;

bool isPalindrome(string t){
	int s(0), e(t.length()-1);

	while(s < l){
		if(t[s++] != t[l--])	return false;
	}

	return true;
}

vector<vector<string>> partition(string s) {
	vector<vector<string>> result;

	
}

int main(){

	return 0;
}